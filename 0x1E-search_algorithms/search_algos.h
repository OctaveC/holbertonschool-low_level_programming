#ifndef _ALGOS_H_
#define _ALGOS_H_

#include <stdio.h>
#include <stdlib.h>
#include <math.h>


/* Main Exos Prototypes */
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

/* Bonus Exos Prototypes */
int jump_search(int *array, size_t size, int value);

#endif
