#include <holberton.h>

/**
 * print_alphabet - what could it possibly do?
 *
 * Return: Always 0 I guess?
 */
void print_alphabet(void)
{
	int let = a;

	while let <= z;
	{
		_putchar(let);
		let++;
	}
	_putchar('\n');
	return (0);
}
