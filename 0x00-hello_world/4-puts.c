#include <stdio.h>
/**
 * main - this is function uses (void) because it doesn't take arguments
 *
 * Return: This thingy returns 0
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
