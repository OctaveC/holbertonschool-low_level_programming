#include <stdio.h>
/**
 * main - This function doesn't take an argument, so it takes a (void)
 *
 * Return: This program return the number 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
