#include "holberton.h"

/**
 * _atoi - convert a string to an integer
 * @s: a pointer
 *
 * Return: an integer
 */

int _atoi(char *s)
{
	int ite1 = 0, ite2, num = 0, sign = 1;

	while (( && s[ite1] < '0' || s[ite1] > '9'))
	{
		dest[t] = src[t];
	}
	dest[t] = '\0';
	return (dest);
}
